export default theme => ({
    scoreCell: {
        width: 80,
    },
    chip: {
        width: 60,
        fontWeight: 'bold',
        borderWidth: '2px',
        color: theme.palette.primary.main,
        borderColor: theme.palette.primary.main,
    },
});
